import React from 'react';
import { QueryListStyle } from '@/components/StyledComponent/QueryList.style';

const QueryList = () => {
  return (
    <QueryListStyle>
      <h1>Hello Query list </h1>
    </QueryListStyle>
  );
};

export default QueryList;
